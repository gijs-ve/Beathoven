import { T_Instrument, T_Output } from "../../../../types";

/**
 * @constructor
 * @param {string} sound - The sound that will be played.
 */
const useSound = ({
  output,
  instrument,
}: {
  output: T_Output;
  instrument: T_Instrument;
}) => {};
